{
  "name": "taskplus",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "node --test",
    "lint": "eslint js/"
  },
  "devDependencies": {
    "eslint": "^10.9.1"
  }
}
